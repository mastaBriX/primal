# Flask 配置
# Flask 密钥，用于会话和 cookie 签名
# 生产环境必须设置强随机密钥，可使用: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Flask 调试模式（生产环境必须设置为 false）
FLASK_DEBUG=false

# Flask 主机地址
FLASK_HOST=0.0.0.0

# 服务端口
PORT=5000

# 应用配置
# Flask 应用文件名
FLASK_APP=app.py

# 质数检查配置
# 最大输入值限制（默认 10 亿，防止 DoS 攻击）
MAX_INPUT_VALUE=1000000000

# 质数检查超时时间（秒，默认 5 秒）
TIMEOUT_SECONDS=5

# 日志配置
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志格式（可选，使用默认格式时可省略）
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Gunicorn 配置（生产环境）
# Gunicorn 工作进程数（0 表示自动计算：CPU核心数 * 2 + 1）
GUNICORN_WORKERS=4

# Gunicorn 超时时间（秒）
GUNICORN_TIMEOUT=30
